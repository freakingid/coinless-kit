# Docs

One folder or file set per module. The leaderboard's three documents:

- `kit-leaderboard-worker-spec.md`   — D1 schema, endpoints, validation, deploy config
- `kit-leaderboard-client-api.md`    — the client module's public API contract
- `kit-leaderboard-deploy-notes.md`  — wrangler setup and smoke tests

Drop those three files into this directory.
